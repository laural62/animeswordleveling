export default function Inscription({userConnected, toggleUser}) {
    console.log(userConnected);
    return (
        <div className="bg-black text-white">
            <h2>S'inscrire !</h2>

            <form className="flex flex-col gap-4 mb-6 mx-auto max-w-[400px]"
            >

            </form>
        </div>
    );
}